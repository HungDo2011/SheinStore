import routesConfig from './RoutesConfig/routesConfig';
import { firebaseConfig, uiConfig } from './FirebaseConfig/firebaseConfig';

const config = {
    routesConfig,
    firebaseConfig,
    uiConfig,
};

export default config;
